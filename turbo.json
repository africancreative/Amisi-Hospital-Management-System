{
    "$schema": "https://turbo.build/schema.json",
    "ui": "tui",
    "tasks": {
        "build": {
            "dependsOn": [
                "^build",
                "@amisimedos/db#generate"
            ],
            "inputs": [
                "$TURBO_DEFAULT$",
                ".env*"
            ],
            "outputs": [
                ".next/**",
                "!.next/cache/**",
                "dist/**",
                "build/**"
            ],
            "env": [
                "NEON_DATABASE_URL",
                "LOCAL_EDGE_DATABASE_URL",
                "JWT_SECRET",
                "CLOUD_SYNC_URL",
                "NODE_ENV",
                "CLOUD_MODE",
                "SYNC_INTERVAL_MS"
            ]
        },
        "generate": {
            "cache": true,
            "inputs": ["prisma/**"],
            "outputs": ["generated/**"]
        },
        "build-mobile": {
            "cache": false,
            "inputs": [
                "apps/mobile/**"
            ]
        },
        "lint": {
            "dependsOn": [
                "^lint"
            ]
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
