{
  "name": "@amisimedos/mobile",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "flutter run",
    "build": "command -v flutter >/dev/null 2>&1 && flutter build web || echo 'Flutter not installed, skipping mobile build'",
    "build:android": "flutter build apk",
    "test": "flutter test",
    "lint": "flutter analyze"
  }
}
